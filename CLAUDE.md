# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

This is the landing page for DeadboltHQ / CertSync / CertHound, hosted on GitHub Pages at `deadbolthq.com`. It is a single static HTML file with no build toolchain, dependencies, or framework.

## Structure

- `index.html` — the entire site: inline CSS, inline content, no external assets
- `CNAME` — GitHub Pages custom domain (`deadbolthq.com`)

## Deployment

Changes pushed to `main` are automatically deployed via GitHub Pages. There is no build step.
